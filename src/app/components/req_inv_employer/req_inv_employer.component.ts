import { Component, OnInit} from '@angular/core';
import {Router} from '@angular/router';

@Component({
    selector: 'req-inv-employer-app',
    styleUrls: ['req_inv_employer.component.css'],
    templateUrl: `./req_inv_employer.component.html`,
})
export class Req_inv_employerComponent {
    check: boolean = true;
}
